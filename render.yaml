services:
  - type: web
    name: bert-sentiment-analyzer
    runtime: python
    startCommand: streamlit run app.py --server.port=$PORT --server.enableCORS=false
    envVars:
      - key: ad0e3181
        value: ""  # Set this environment variable via Render's dashboard for security
    plan: free
